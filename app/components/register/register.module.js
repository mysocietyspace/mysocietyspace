/**
 * @ngdoc overview
 * @name Login
 * @description
 * Click on the internal link should open reltaed Accordion
 */

import RegisterControler from './register.controller';

let registerModule = angular.module( 'mss.register', [] )
.controller( 'RegisterControler', RegisterControler );
export default registerModule = registerModule.name;
