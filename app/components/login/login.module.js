/**
 * @ngdoc overview
 * @name Login
 * @description
 * Click on the internal link should open reltaed Accordion
 */

import LoginControler from './login.controller';

let loginModule = angular.module( 'mss.login', [] )
.controller( 'LoginControler', LoginControler );
export default loginModule = loginModule.name;
